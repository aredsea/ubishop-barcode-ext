# 검수 원장 (REVIEW LEDGER)

이 저장소의 외부·내부 검수 결과를 남긴다. **다음 검수 브리프에 아래 「누적 판정」 절을 그대로 붙여 넣는다.**
안 그러면 이미 근거를 대고 기각한 지적을 매 라운드 다시 받아 비용만 늘어난다.

검수자에게 함께 넘길 한 줄: *"아래는 이미 판정된 지적이다. **같은 것을 다시 내지 마라.**
다만 **근거가 달라졌다고 판단하면 그 근거를 적고** 다시 내도 된다."*

🔴 **검수 중에는 대상 파일을 고치지 마라.** 브리프에 `git hash-object` 값을 박고 검수자가 시작·종료에
대조하게 한다. 1회차에 검수 중 파일이 3번 바뀌어 판정이 어긋난 적이 있다.

---

## 누적 판정 — 다시 묻지 마라

| # | 라운드 | 낸 곳 | 지적 요약 | 판정 | 근거 |
|---|---|---|---|---|---|
| 1 | 1R | Luna | 테스트가 입력칸 **총 개수만** 세어 id 바꿔치기를 놓친다 | **채택·수정** | 변이 재현. id 마다 정확히 1개를 센다 |
| 2 | 1R | Luna | 테스트가 `bcRead(` 문자열만 봐서 `bcRead()` 도 통과 | **채택·수정** | 변이 재현. 인자·대상 id 까지 검사 |
| 3 | 1R | Opus 5 | 잘린 바코드가 형식 검증 없이 조회되고, 한글이 화면에서 사라져 **원인 진단 신호 소멸** | **채택·수정** | `bcRead` 가 `null` 로 조회를 멈추고 한/영을 지목 |
| 4 | 1R | Opus 5 | `selectionStart` 널 가드가 테스트로 안 고정(`>= 0` 은 캐럿 0 도 통과) | **채택·수정** | 캐럿 위치를 정확히 assert |
| 5 | 1R | Opus 5 | 배선 "순서" 주석이 코드 동작과 불일치 | **채택·수정** | 이벤트 타입이 달라 순서 무관. 주석 정정 |
| 6 | 1R | Opus 5 | 유비샵 **페이지 자체**의 바코드 칸이 필터 밖 | **채택·구현** | 사장님이 "모든 유비샵 입력 칸"으로 확정 |
| 7 | 1R | Opus 5 | `run`/`msRun`/`dcmRun` 에 6자리 형식 가드 없음 | **채택·구현** | 사장님이 "항상 6자리"로 확정 |
| 8 | 1R | Opus 5 | 대문자 통일 · 리스너 누수 · `input` 재귀 · 선택영역 · 스캐너 접미문자 | **문제 없음** | 하류가 `toUpperCase`, `innerHTML` 교체라 누수 없음, 스크립트 value 대입은 `input` 미발생 |
| 9 | 2R | Luna | 페이지 칸에 실행 직전 검증이 없어 **잘린 값이 유비샵 네이티브 폼으로 제출**될 수 있다 | 🔴 **기각** | ① 지시는 "입력"에 관한 것이고 **유비샵 폼 submit 가로채기는 요청 범위 밖** ② 페이지 검색칸의 부분 검색은 **정상 사용**일 수 있어 차단이 오히려 회귀 ③ 결과가 화면에 보이는 **조회**라 파괴적이지 않다 ④ 확장 자신의 파괴적 흐름은 전부 `bcRead`+6자리 가드를 지난다. 대신 **흔적(title·console)** 을 남기는 것으로 대응(#14) |
| 10 | 2R | Luna | 스킨을 **나중에 끄면** 이미 붙은 페이지 리스너가 계속 값을 고친다 | **채택·수정** | gate 를 리스너 안에서 매번 평가 |
| 11 | 2R | Luna | `\s` 가 제로폭(U+200B)을 못 잡아 그것만 붙어도 경고 | **채택·수정** | 목록 대신 판정을 뒤집어 `bcLostVisible` 도입 |
| 12 | 2R | 자체 변이 | 소스 대조 assert 가 **주석 처리된 줄도 통과** | **채택·수정** | 주석을 걷어낸 `LIVE` 로 대조 |
| 13 | 3R | Opus 5 | **라벨 폴백이 바코드와 무관한 한글 칸을 물어 글자를 지운다**(P1) | **채택·수정** | 격리 재현: readOnly 바코드칸 화면에서 `custName` 이 잡혀 한글 입력이 빈 값이 됐다. 폴백 제거 + 부활 가드 |
| 14 | 3R | Opus 5 | 페이지 칸에 "지웠다"는 흔적이 없다 | **채택·수정** | `title` + `console.warn`(폼 submit 은 안 막는다 — #9 참조) |
| 15 | 3R | Opus 5 | 비가시 문자 목록이 계속 샌다(U+200E/F·U+00AD·U+061C·U+180E·U+034F·U+2066) | **채택·수정** | 열거를 버리고 `bcLostVisible` 로 판정 반전 |
| 16 | 3R | Opus 5 | 테스트 stub 이 셀렉터·라벨 인자를 무시해 변이가 생존 | **채택·수정** | 셀렉터 부분일치를 실제로 흉내내는 stub |
| 17 | 3R | Opus 5 | `setTimeout` 재시도 · `scrub` 조기 return 이 무검증 | **채택·수정** | 소스 대조 + `setSelectionRange` 호출 수 |
| 18 | 4R | Luna | 재시도(900ms) 이후 **동적으로 그려진** 페이지 칸이 배선 안 됨 | **채택·수정** | MutationObserver 가 새 바코드 칸을 감지해 재배선 |
| 19 | 4R | Luna | 테스트의 `id="…"` 부분일치가 **`data-id="…"` 에도 걸린다** | **채택·수정** | 변이 재현(생존). 속성 경계를 보도록 수정 |
| 20 | 4R | Luna | CSS 로 숨겨진(`display:none`) 텍스트 칸을 제외하라 | 🔴 **기각** | 배선 자체는 무해하다 — 필터는 `input`/`compositionend` 에서만 돈다. 숨은 칸은 사용자 입력을 못 받고, 페이지 스크립트가 `.value` 를 세팅하는 것은 **`input` 이벤트를 발생시키지 않는다**(HTML 표준). 스크립트가 일부러 dispatch 하는 근거는 제시되지 않았다. 반대로 `offsetParent` 로 거르면 접힘 패널의 칸을 놓친다 |
| 21 | 4R | Luna | 결합 부호(U+20DD 등 `\p{M}`)가 지워져도 조용히 통과 | 🔴 **기각** | 실측: `2112B`+U+20DD+`J` → `2112BJ` 로 **원 바코드가 그대로 복원**되고 6자라 조회에 아무 영향이 없다. 반대로 `\p{M}` 을 넣으면 U+034F 같은 비가시 결합문자에 오경고가 나서 #15 의 열거 지옥으로 되돌아간다 |
| 22 | 5R | Opus 5 | **사이드바에서 한/영 경고가 사실상 안 뜬다**(compositionend 가 먼저 털어서 `bcRead` 는 깨끗한 값을 본다) | **채택·수정** | 격리 재현: 사용자가 보는 유일한 메시지가 "6자리를 입력하세요" 였다. `bindSidebarBcInput` 으로 **털린 즉시** 상태줄에 원인을 적는다 |
| 23 | 5R | Opus 5 | 배선 가드가 텍스트 대조라 `if (false)`·**블록주석**으로 죽여도 통과 | **채택·수정** | 블록주석 제거 후 대조 + `if (false)` 트립와이어 |
| 24 | 5R | Opus 5 | 6자리 가드에서 `return;` 만 빼도 통과(경고만 하고 조회 진행) | **채택·수정** | 가드 블록 안 `return;` 까지 대조 |
| 25 | 5R | Opus 5 | `bcRead(inp, null)` 로 바꿔도 통과 → 경고가 통째로 사라짐 | **채택·수정** | 2번째 인자가 그 칸의 상태 setter 인지까지 대조 |
| 26 | 5R | Opus 5 | `\p{S}`(기호) 분류 · 타입 화이트리스트 · 낡은 title 이 무검증 | **채택·수정** | Sm/Sc/Sk/So 케이스 · password/number 미끼 · title 정리 assert |
| 27 | 5R | Opus 5 | 라벨 폴백 제거로 실제 칸을 놓치는가 / idx 쉼표값 / 누수 / 6자리 회귀 | **문제 없음** | 저장소가 아는 실측 필드명은 `searchBarcode` 뿐이고 셀렉터에 걸린다. 6자 계약은 `BARCODE_RE` 와 일치하고 runner 호출부는 각 1곳뿐 |
| 28 | 매입처-1R | Luna | 스킨을 끈 뒤에도 입력 리스너가 계속 필터링하고, 다시 켜도 초기화 안 됨 | **채택·수정** | `ubSkin` 은 `ALWAYS_ON` 밖이라 실제로 꺼진다. `popup.js` 는 reload 없이 토글. 원장 #10 과 같은 계열 |
| 29 | 매입처-1R | Luna·Opus 5 | 불완전 프리페치 캐시가 '전체'로 승격돼 검색이 0건을 낸다 | **채택·수정** | span 미발견 시 `total=0` → `0>=0` 항상 참 → 1페이지에서 break. 루프 후 대조 없음 |
| 30 | 매입처-1R | Opus 5 | 폼 직렬화가 기존 `statisFormParams` 보다 약함(submit/image/file 미배제) | **채택(하드닝)** | ⚠️ **오늘 재현 안 됨** — form1 실측에 해당 타입 없음. 단일 소스 원칙 이탈이라 수용 |
| 31 | 매입처-1R | Opus 5 | 신규 테스트가 이 diff 의 회귀를 **하나도 못 잡는다** | **채택·수정** | 변이 3종을 실제로 걸어 24 pass 생존 실증. 배선 테스트 추가 |
| 32 | 매입처-1R | Opus 5 | 복제본 재렌더로 행 체크상태·핸들러가 소실된다 | 🔴 **기각** | **실측**: 두 화면 데이터 행에 `input`/`checkbox` **0개**, 링크는 전부 인라인 `javascript:view('338')` 라 `cloneNode` 에 보존, 행/셀 `onclick` 0개. **잃을 상태가 없다.** 단 스냅샷을 원본 노드로 잡는 (a)만 하드닝 수용 |
| 33 | 매입처-2R | Luna | 지연 콜백(120ms 디바운스·프리페치 `.then`)에 게이트가 없어 스킨을 꺼도 다시 필터링된다 | **채택·수정** | 내 1R 수정이 만든 꼬리. 게이트를 진입점에만 걸었었다 |
| 34 | 매입처-2R | Opus 5 | 1R Critical 수정이 테스트에 전혀 고정 안 됨(변이 4종 생존) | **채택·수정** | 재현 확인. **근본 원인은 더 깊었다** — `runWiringChecks` 가 변이 테스트 안에서만 호출돼 **실제 소스에는 한 번도 적용되지 않았다.** 최상위 테스트로 닫음 |
| 35 | 매입처-2R | Opus 5 | `stripComments` 우회 — 주석 속 decoy 가 검사를 통과 | **채택·수정** | 재현. 줄 단위 문자열-상태 스캐너로 교체. ⚠️ 파일 전체 스캐너는 정규식 `/^\/jun\//` 의 `\//` 를 주석 시작으로 오인해 뒤가 날아간다(실측) → 줄 단위로 가둠 |
| 36 | 매입처-2R | Opus 5 | 재-ON 시 고정헤더가 `display:none` 에 갇혀 스크롤 전까지 안 보임 | **채택·수정** | ON 분기에 `flSyncFix()` 추가 |
| 37 | 매입처-2R | Opus 5 | 다중 페이지 병합이 중복 캐시를 승격시킬 수 있다 | **채택(하드닝)** | ⚠️ 오늘 재현 불가(324 < 500 이라 1페이지에서 끝). `FL_PREFETCH_PAGE_SIZE` 상수 + 마지막 페이지 판정 |
| 38 | 매입처-3R | DeepSeek v4-pro | **P0** — DOMParser 문서 노드를 `cloneNode` 후 `appendChild` 하면 `WrongDocumentError` 로 필터가 죽는다 | 🔴 **기각** | **라이브 실측으로 반증**: throw 없음, 삽입 후 `ownerDocument` 가 `document` 로 바뀜(**DOM4 자동 adopt**). `WrongDocumentError` 는 DOM Level 2 규칙이다. 실제 경로도 2개 라운드에서 9행 정상 렌더(죽었다면 0행) |
| 39 | 매입처-3R | Opus 5 | 배포 배관(버전 미상향·`shell-files.json` 해시 낡음)이면 매장에 안 나간다 | **채택·처리** | `background.js:254` 가 버전 비교로 동기 트리거. 4.1.8 상향 + 인덱스 재생성 + `loader-integrity` 통과 |
| 40 | 매입처-3R | Opus 5 | 문자열 리터럴 decoy·첫 문장 고정의 양방향 한계(Nit) | **인지·주석화** | 사고로 나오는 형태가 아니라 감수. 테스트에 한계를 주석으로 남겨 다음 사람이 헤매지 않게 함 |
| 41 | 매입처-3R | Opus 5 | 낡은 주석 `pageSize='1000'`(실제 500) | **채택·수정** | 주석 2줄 정정 |
| 42 | 매입처-3R | Opus 5 | F4 가 라이브 요청 파라미터를 바꿨는데 재검증 목록에서 빠졌다(Nit) | 🔴 **기각(이미 검증됨)** | 2R 브라우저 실측이 **이미 `pageSize=500` 으로** 돌았다 — `프리페치(pageSize=500): 324행 / total=324` |
| 43 | 취소-1R | Terra | 배치 진행 중 툴바 [일괄취소] 재클릭이 진행 창을 지워 [중단] 을 잃는다 | **채택·수정** | 진입점 `cBatchBusy` 가드 + `dataset.ubRunning` 창 보존 |
| 44 | 취소-2R | Terra | 승인창 [취소 진행] 이 `isTrusted` 를 안 봐 페이지 스크립트 `.click()` 으로 쓰기 시작 가능 | **채택·수정** | 핸들러 첫 줄 untrusted 반환 + 가짜 DOM 테스트 |
| 45 | 취소-2R | Terra | 건별 fresh sKey 불변식이 테스트에 미고정 | **채택·수정** | 고유 키 스텁 + URL 대조(뒤에 #48 로 원자화) |
| 46 | 취소-3R | Terra | sKey 대기(최대 8초) 중 남이 상태를 바꿔도 취소 GET 이 나간다 | **채택·수정** | dispatch 직전 재검증 → #48 에서 "상태·키 한 응답" 구조로 대체 |
| 47 | 취소-3R | Terra | document-local `cBatchBusy` 라 교차 탭·frame 중복 실행 → SW 전역 잠금 요구 | 🔴 **기각** | 재현하려면 같은 PC 두 탭에서 사람이 같은 행을 각각 승인해야 하고, 그래도 각 탭이 쓰기 직전 재조회로 O-- 를 확인해 두 번째 GET 은 이미 OC- 인 주문에 간다(오취소·데이터 손실 아님). 네이티브 UI 도 같은 노출. SW 잠금은 잠금 누수·TTL 등 새 위험이 더 크고 운영 중인 작업C 도 같은 설계 |
| 48 | 취소-4R | Terra | 대기 중 게이트 OFF/[중단] 이어도 취소 GET 이 나간다 | **채택·수정** | dispatch 직전 게이트·isAborted 재확인 |
| 49 | 취소-O1 | Opus 5 | 3-1 중단 시 요약이 '처리 완료'/미처리 수 오표시 | **채택·수정** | `processed` 되돌림 + catch 경로 '실행 오류' 실패 기록 |
| 50 | 취소-O1 | Opus 5 | 실행 중 `cBatchBusy` 세움이 테스트 미고정(변이 생존) | **채택·수정** | 재조회 시점 busy 관측 테스트 |
| 51 | 취소-O1 | Opus 5 | 별도 GET 으로 받은 sKey 와 중간 목록 렌더의 상호작용(키 무효화 가설) | **채택·구조로 해소** | `fetchOrderRow` 가 응답의 sKey 를 함께 반환 → 상태·키를 한 응답에서. 라이브 읽기 실측: POST 렌더 응답의 첫 sKey 출현이 `del()` 의 것 |
| 52 | 취소-O1 | Opus 5 | 닫기 버튼 라벨 '취소' 가 주문취소와 혼동 | **채택·수정** | '닫기' 고정 |
| 53 | 취소-O2 | Opus 5 | `fetchOrderRow` 의 sKey 배관이 테스트 미고정(변이 2종 생존) | **채택·수정** | 실제 함수를 가짜 fetch/DOMParser 로 돌리는 테스트 3건 |
| 54 | 취소-X1 | DeepSeek v4-pro | 지적 없음 | — | CJK 오염 0 |
| 55 | 취소-F1 | Fable 5.1 | 취소 `seq`(idx 값)와 `del()` 인자 동일성 미실측 + 같은 응답의 [취소] 링크를 안 봐 라벨이 틀리면 fail-closed 아님(P1) | **채택·수정** | 라이브 읽기 실측 332/332(프로덕션 경로 `tr.outerHTML` 로 327/327) + `ccRowCancelSeq` 로 링크 인자 EXACT 일치 가드 |
| 56 | 취소-F1 | Fable 5.1 | 무필터 재조회가 OC- 행을 돌려준다는 성공 판정 계약 미실측 | **채택·실측** | seq 389315(9/9): 1행·'주문취소'·링크 없음 |
| 57 | 취소-F1 | Fable 5.1 | 툴바 주입·init/onChanged 배선·untrusted 클릭 / 진행 중 배경 클릭 가드가 테스트 미고정 | **채택·수정** | 가짜 툴바 DOM·소스 대조·미해결 Promise 배치 테스트, 변이 4종 KILL |
| 58 | 취소-F1 | Fable 5.1 | 샌드박스 타이머 단축이 8000ms abort 타이머까지 0 으로(Nit) | **채택·수정** | `ms === 1500` 으로 좁힘 |
| 59 | 취소-F2 | Fable 5.1 | onChanged 배선 테스트가 주석 문자열·인접 줄에 묶여 무해한 편집에 깨진다(Nit) | **인지·유지** | 시끄럽게 깨지는 쪽이 안전. 느슨하게 풀 때 #57 의 구멍이 되돌아오니 `if (changed) {` 블록 전체로 자를 것 |
| 60 | 포커스-1R | Luna | `app-files.json` 해시 미갱신 — loader 가 번들 전체를 거부해 배포가 안 되거나 신규 설치가 안 뜬다(P1) | **채택·처리** | 맞다. 검수 전에 인덱스를 안 올린 내 순서 문제. `node build-app-index.js 2.9.10` 로 갱신 |
| 61 | 포커스-2R | Luna | 같은 pathname 의 `master_item`·`master_item_image` 탭에도 걸리니 `tcode=master_item_k` 로 제한하라(P2) | 🔴 **기각** | 요청은 "기초상품관리 페이지" 전체이고 세 탭(기본정보/품위정보/이미지보기)이 **같은 검색 폼**을 공유한다. 칸이 없으면 `apply` 가 스스로 빠진다 |
| 62 | 포커스-2R | Luna | 번들이 비동기로 늦게 붙는 사이 사용자가 **빈 칸·셀렉트**에 먼저 들어가 있으면 첫 타이머가 포커스를 뺏는다(P2) | **채택·수정** | 빈 값 조건을 버리고 `activeElement` 가 어떤 컨트롤이든 잡혀 있으면 취소(`isControl`). 페이지 자체는 로드 후 포커스를 안 준다(실측 body) |
| 63 | 주문-1R~2R | Terra | 완료 POST 성공 뒤 확인 GET 예외가 완료된 줄을 지움 · 되돌리기 예외가 runAll 까지 reject · 완료/줄 POST 응답 유실 · 주문번호 빈 행 | **채택·수정** | `res.completed/completing`(완료 POST 이후 실패는 삭제 금지 fatal) · `res.lineUnknown` · 빈 주문번호 행 분리+차단 |
| 64 | 주문-3R | Terra | 줄 POST 예외 뒤 skipped 로 계속(지연 커밋) · 스위치 OFF 뒤 열린 패널 실행 | **채택·수정** | 줄 POST 예외는 무조건 `line_unverified` fatal · run() 진입 시 storage 재확인 |
| 65 | 주문-3R | Terra | 완료 직전 **서버측 compare-and-complete/배타 잠금**, 불가능하면 자동 완료 금지(P1) | 🔴 **부분 채택·나머지 기각** | 유비샵(Struts 폼 POST)에 원자 연산이 없다. "자동 완료 금지"는 사장님 결정(실행 중 조작 금지)으로 기각. 완료 직후 전표 목록 사후 검출(`foreign_line_completed`) + 쓰기 직전 plain GET 대조(#73)로 대응 |
| 66 | 주문-4R~6R | Terra | 수령자 불일치 · 이중 클릭 · 새 줄 귀속 불명 · 미등록 판매처 · 수동 보정 시 미해석 토큰 · 빈 주문장 열림 · 완료 대조가 code 만 | **채택·수정(7건)** | groupMismatch · `S.starting` 선점 · rowmismatch fatal · oiApplyMarket · optOverride · 첫 줄 tradeJun 빈값 요구 · 등록 응답 행 스냅샷 전체 일치 |
| 67 | 주문-7R | Terra | 되돌리기 직전 세션 확인 없음(완료된 전표를 지울 수 있음) · `shell-files.json` 미재생성 | **채택·수정 / 배포 단계** | plain state 대조 후 다르면 삭제 없이 fatal · 인덱스는 검수 중 파일 변경 금지라 배포 직전 재생성(`loader-integrity`) |
| 68 | 주문-8R~9R | Terra | 파일 연속 선택 시 늦은 파싱이 표를 덮음 · 매핑 항목 seq/code 미검증 · 21줄 이상 주문장 중간 fatal(pageSize 20) | **채택·수정** | fileGen 토큰 · `oiValidMapEntry` · `OI_MAX_LINES=20` 쓰기 전 차단 |
| 69 | 주문-10R | Terra | 코드만 다른 유일 새 줄을 lineUnknown 처리 · 되돌린 뒤 plain state 미확인 · 전표 미확인을 done 으로 | **채택·수정** | code_mismatch 는 내 줄로 기록 후 일반 되돌리기 · rollback 후 `state()` 비움 확인 · '완료 응답 성공, 전표 미확인' 경고 |
| 70 | 주문-10R | Terra | 고객 등록 응답 유실을 fatal 로(P2) | 🔴 **기각** | 같은 고객이 같은 배치에 다시 나오면 이름 검색이 등록분을 찾아 재사용 → 중복 고객 없음. 고객 등록은 주문 쓰기가 아니다 |
| 71 | 주문-11R~14R | Terra | 소수 판매가 반올림 · 정산 0 차단 · 재매핑/매핑 지우기 시 접미·폴백 소실 · 수동 색상 미학습 · 숫자형 휴대폰 셀 · 세션 만료 리다이렉트를 성공으로 오판 | **채택·수정** | 정수만 · `oiMoney0` · 메타 승계·`priorMeta`·colorFallback 학습(+`oiFallbackColor` 미적용 결함 자체 발견) · 브리지 `raw:false`+`numericPhoneRows` · 어댑터 `assertUbdstore` |
| 72 | 주문-O1 | Opus 5 | 완료됐을 수 있는 fatal 주문장이 체크된 채 장부 없이 남아 재클릭 시 **중복 주문장**(P1) | **채택·수정** | `oiPostRunState` 순수 함수 — 체크 해제 + 장부 unverified('이전에 넣음' 경고) |
| 73 | 주문-O1 | Opus 5 | 쓰기 경로(둘째 줄 이후·완료 직전)에 plain GET 세션 대조 없음 — 명시 tradeJun GET 은 완료돼도 행을 보여줘 스텁이 사각을 가렸다(P2) | **채택·수정** | `assertTradeOpen` + 스텁 `srv.closed` 로 실제 서버처럼 모델링 |
| 74 | 주문-O1 | Opus 5 | 가드 3개 테스트 미고정(변이 생존) · 휴대폰 재조립이 다른 번호를 만듦 · 장부 덮어쓰기 · 실행 중 매핑표 변경 유실 · 브리지 휴대폰 열 과검출 | **채택·수정** | 변이 KILL 테스트 · 원문 하이픈 유지/국가코드 검토 · 장부 병합 저장 · onChanged 반영 · 검출 열 한정 |
| 75 | 주문-O2 | Opus 5 | **O1 수정이 만든 회귀** — 세션 대조 GET 이 form10 GET 과 완료 POST 사이에 끼어 완료 POST 의 sKey 가 직전 GET 의 것이 아니게 됨(P1) | **채택·수정** | 대조를 form10 GET 앞으로. GET 마다 고유 sKey 를 내는 스텁으로 모든 쓰기 POST 가 마지막 발급 키를 쓰는지 고정. 스펙 §4 에 "그 GET 과 POST 사이에 다른 GET 을 끼우지 않는다" 명문화 |
| 76 | 주문-O2 | Opus 5 | `trade_switched` 가드가 테스트 밖이고 code_mismatch 뒤 · lineUnknown 이 장부에 안 남음 · 공백 구분 휴대폰 · 미확인 장부 표시(P2·P2·Nit·Nit) | **채택·수정** | 순서 교정+테스트 · mayRemain 에 lineUnknown · 토막 유지 · 표시 |
| 77 | 주문-O3 | Opus 5 | 응답 tradeJun 빈값 경로를 명시 사유로(Nit) · deleteLines 의 sKey 는 어댑터 구조상 안전하나 스텁 밖(Nit) | **채택·수정 / 인지·기록** | `trade_missing` lineUnknown fatal · 어댑터 변경 시 그물이 없다는 기록만 |
| 78 | 주문-O3 | Opus 5 | 키 발급 GET 에서 tradeJun 명시를 빼면 대조 GET 이 불필요(창 0)해진다 | **보류(미실측)** | 전제 2개(세션 반영 GET 의 form1/form10 렌더)가 라이브 미실측. 다음 라이브 읽기 때 재서 결정. 지금 구조는 스펙 §4 와 일치 |
| 79 | 주문-X1 | DeepSeek v4-pro | 지적 없음(재시도). `oiReadForm10` 구간 경계 정규식이 이론상 form20 에 걸린다는 관찰 | **문제 없음** | 구간은 form10 시작에서 *그 뒤* form 태그까지라 어느 폼에서 잘려도 form10 필드는 전부 안에 있고 실제 페이지에 form20 없음. CJK 오염 0 |
| 80 | 주문-F1 | Fable 5.1 | 되돌리기가 내 줄 **밖**까지 지워도(서버 계약이 idx 단독 삭제가 아니면) 남의 줄 소실이 skipped 로 통과해 배치가 계속(P1 조건부) | **채택·수정** | 어댑터가 삭제 직전 행 목록 `before` 를 돌려주고 core 가 남의 줄 소실 시 `rollback_overreach` fatal, `before` 없으면 `rollback_unverifiable` fatal. 되돌리기 라이브 미실측이라 계약이 틀려도 fail-closed |
| 81 | 주문-F1 | Fable 5.1 | 실행 중에도 검토 표의 선택·검색·매핑 지우기가 살아 있어 enrich/검색 GET 이 실행기의 sKey GET→POST 사이에 끼어들 수 있음(P2) | **채택·수정** | onClick/onChange/enrich 게이트 + 줄·마켓·매핑표 컨트롤 disabled(배선 테스트 고정) |
| 82 | 주문-F1 | Fable 5.1 | `remain.some(내 orderSeq)`·진입 가드 rows>0·행 수 대조가 테스트 밖(변이 생존)(P2·Nit) · importMap 뒤 enrich 누락(Nit) | **채택·수정** | 테스트 3건(변이 4종 각 1건 실패로 KILL) · importMap 뒤 `enrich()` |
| 83 | 주문-F1 | Fable 5.1 | 검토 표에서 품위/색상 칸을 비우면 파싱값으로 되살아나 "마스터 기본값 사용"을 표현할 수 없다(Nit) | 🔴 **기각** | 값이 화면에 그대로 보이고 잘못된 값이 조용히 나가지 않는다. 그 표현은 요청 밖 |
| 84 | 주문-F2 | Fable 5.1 | F2 수정의 빈틈 — 파일 로드 직후 **이미 진행 중인** enrich 가 [등록 시작] 뒤에도 남은 요청을 보냄(진입 게이트만 있었다)(P2) | **채택·수정** | `S.enriching` 프라미스(겹치면 직렬화)를 run() 이 끝까지 기다린 뒤 running · 요청마다 게이트 · 조회 중 실행 버튼 잠금 |
| 85 | 주문-F3~F4 | Fable 5.1 | 시작 대기(`S.starting`) 중 핸들러가 열려 confirm 한 집합≠실행 집합 가능 · 조기 return 시 삼킨 체크박스 표시 불일치(Nit·Nit) | **채택·수정** | 게이트에 `S.starting` 포함 + confirm 전 `jobs` 스냅샷을 그대로 실행 · 조기 return 시 `render()` |
| 86 | 회전-1R | GLM-5.2 | 지적 없음 | — | 5관점·return 경로 전부 셈 |
| 87 | 회전-O1 | Opus 5 | 서버 거부(URL msg)인데 열린 회전입고장에 남은 **이전 실행의 행**을 폴러가 찾아 초록 '등록' 이 빨강을 덮는다(P2) | **채택·수정** | 같은 바코드 재스캔→거부 재현. msg 가 있으면 찾은 행은 이번 제출 것이 아니므로 보관함·상태줄 모두 무시(`rotServerMsg()` 가드) |
| 88 | 회전-O1 | Opus 5 | 재렌더 시 폴링 결과 소실 · `<br>` 뒤 공백 의존 · 테스트 그물(초기값·정규식 완화·`r !== tr`) · 낡은 주석 · 저장 실패를 '등록' 으로 · 남의 화면 flag 오경고(Nit 6) | **채택·수정** | `rotLastResult` 재적용 · 자식 노드 공백 join · 테스트 3종(스텁 `rows=[tr,hdr]`) · 주석 · `stkRecentAdd` true/false · `UB_ROTATE_LAST` 대조 |
| 89 | 회전-O1 | Opus 5 | 본사반품확인 POST 가 5xx 로 리다이렉트 없이 끝나면 `{ok:true}`(사전 존재 관찰) | **범위 밖·기록** | 이번 diff 이전과 같고 서버가 회전입고를 거부하므로 안전망 있음. 손대면 별도 회차 |
| 90 | 회전-O2 | Opus 5 | 거부 플래그가 렌더 경로에서만 세워짐 · 셀 앞 주석 노드가 토큰이 됨 · 새 실행 시 `rotLastResult` 미초기화(Nit 3) | **채택·수정** | `rotServerMsg()` 가 URL 을 직접 읽어 배선·훅이 같은 출처 · `nodeType !== 8` 제외 · `rotateRun` 진입 시 null |
| 91 | 회전-X1 | DeepSeek v4-pro | 지적 없음 | — | CJK 오염 0, 탐색 최소화 지시로 $0.06 |
| 92 | 검색-1R | Luna | 검색 뒤 칸을 비우고 Enter 하면 `\|\| l.q` 폴백으로 이전 검색어 재검색(P2) | **채택·수정** | 입력칸이 있으면 그 값만(빈 값은 빈 값) + 동작 테스트 |
| 93 | 검색-1R | Luna | 배선 테스트가 소스 정규식뿐이라 동작 회귀를 못 잡는다(P2) | **부분 채택** | `searchLine` 을 잘라 가짜 S/E/render 로 실제 실행하는 테스트 추가. 클릭·Enter 래퍼는 소스 대조 유지(DOM 이벤트 하네스는 과함) |
| 94 | 검색-2R | Luna | 검색 A 진행 중 B 재검색 시 늦은 A 응답이 덮는 경합(P2) | **채택·수정** | 줄 단위 세대 토큰 `l.qGen` + 응답 순서 뒤집는 테스트. ⚠ T1 상한(2R) 도달 — 이 수정은 외부 재검수 없이 자체 검증으로 닫음(사장님께 보고) |
| 95 | 사은품-1R | Luna | 지적 없음 | — | 파싱→검토→페이로드 연결·일반 상품 0 차단 확인 |
| 96 | 리디자인-1R | GLM-5.2 | enrich 재시작 직후 스트립이 이전 카운터를 보임(P3) | **채택·수정** | enrich() 진입 시 progInit + body 시작 renderSoft |
| 97 | 리디자인-O1 | Opus 5 | `MARKET_OPTS` 에서 오늘룩 누락(회귀, P2) · 조회 중 재렌더가 파일 input 을 떼어냄(P2) · 미등록 판매처 접미/마켓 입력이 onChange 폴스루로 지워져 [적용]이 한 번도 안 됐음(기존 결함, P2) · 새 배선 변이 11종 생존(P2) · 판정 뒤 중복이 된 주문장이 체크된 채 남는데 배너는 "풀어 두었다"(P2) · chkall 이 손으로 켠 중복을 끔(Nit) · 조회 끝 렌더의 입력 소실(Nit) | **채택·수정(7)** | 오늘룩 복원 · render/renderBody 분리 · mkt 필드 조기 return · toRunOrder 실행 테스트+run 테스트(sig·사은품 0원)+소스 핀(변이 15종 KILL) · wasDup 전환 순간 재기본값+배너 두 문구 · chkall 은 중복을 건너뜀 · (지연 렌더는 O2 에서 철회) |
| 98 | 리디자인-O1 | Opus 5 | 서명이 파일 수량 기준(편집 수량 아님) · 키당 서명 하나(Nit 2) | 🔴 **기각·기록** | 같은 파일 재등록을 잡는 것이 목적이라 파일 기준이 맞다 · 키당 하나는 감수(주황 칩이 남는다) |
| 99 | 리디자인-O2 | Opus 5 | O1 Nit 수정(renderWhenIdle)이 만든 회귀 — 호출부가 어차피 render() 를 이어 부르고, 매달린 focusout 리스너가 다음 포커스 이동에서 전체 렌더를 일으켜 입력·포커스·클릭을 날림(P2) · 조회 중 표 갱신이 체크박스/[적용] mousedown~mouseup 사이에 끼어 클릭 소실(P2) · 머리글 체크 상태 식이 dup 체크를 셈(Nit) · `!o.result` 가 done 아닌 결과까지 덮음(Nit) | **채택·수정(4)** | 지연 렌더 철회(조회 끝 렌더는 즉시, 입력 1회 소실 감수) · renderSoft 가드 = 표 영역 포커스(태그 불문) · nChkAll · o.ledgered |
| 100 | 리디자인-O3 | Opus 5 | 지적 없음(X2/X3/X8·Y1·Y2 재현 닫힘) | — | 통합 가능 |
| 101 | 리디자인-X1 | DeepSeek v4-pro | (사장님 지시 "검수 너무 많이 하지마" 로 실행 중 중단) | — | 교차 생략 |
| 102 | 고객등록-O1 | Opus 5 | P2-1 비고(remark)가 서버로 가는 어댑터 고리(`orderimport-erp.js` `fixed.remark`)가 테스트 밖 — `remark: ''` 로 되돌려도 107 통과 | **채택·수정** | wiring 에 시그니처+`slice(0,200)` 소스 핀(변이 → FAIL 확인). 원장 #93 선례대로 하네스 대신 핀 |
| 103 | 고객등록-O1 | Opus 5 | Nit-1 사은품 줄에 판매가가 적혀 있으면 지인소개 비율이 1~2%p 움직인다(100,000/41,000 + 사은품 3,000/0 → 60.2%) | **기각(기록)** | 규칙은 "판매금액 대비 정산금액" — 파일 값 그대로 합산이 파일 합계 행과 같은 셈. 스펙 §5d 문구를 "둘 다 0" 에서 "파일 값 그대로" 로 정정, 그 케이스를 테스트로 고정. 사장님이 다르게 원하면 `l.gift` 줄 제외 한 줄 |
| 104 | 고객등록-O1 | Opus 5 | Nit-2 검토 표에서 편집한 판매가·수량은 지인소개 판정에 안 들어간다(파일값 null → 원래 마켓) | **기각(기록)** | 방향이 보수적(원래 마켓). 스펙 §5d 에 명시. 편집값 기준을 원하면 spec 을 넘기도록 바꾼다 |
| 105 | 고객등록-O1 | Opus 5 | Nit-3 고객 조회 실패(`unknown`) 칩에는 지인소개 표시가 안 붙는데 실행은 적용된다 · `ref` 조건 반전 변이가 산다 | **기각(기록)** | 표시 전용. 실행 규칙은 `oiClientJob` 이 실행 테스트로 고정돼 있다 |
| 106 | 고객등록-1R | Luna | 지적 없음 | — | 통합 가능(외부 1R 충족) |
| 107 | 사슬취소-1R | Terra | P1 선택취소 전 링크 orderSeq(2번째 인자)를 행과 대조하지 않음 | **채택·수정** | `ccNextStep` I-- 에 `args.orderSeq === seq` EXACT(취소 링크 가드와 같은 규율) |
| 108 | 사슬취소-1R | Terra | P2 출고전표 조회 POST 에 타임아웃 없음 → 정체 시 배치가 영영 busy | **채택·수정** | `dcmPostRaw/dcmDelete` 선택 `signal`, `ccFindDelivRow/ccDoDelivDelete` 에 `ASG_FETCH_MS` abort(사이드바 출고취소 무변경) |
| 109 | 사슬취소-O1 | Opus 5 | P1 I-- 링크 바코드 대조가 같은 출처(`assignedBarcode` 도 링크에서 옴)끼리라 항상 참 — 테스트는 프로덕션이 못 만드는 입력으로 통과 | **채택·수정** | 상태 셀 괄호 바코드 `ccCellBarcode(row.text)` 와 대조. 스텁도 `text` 에 괄호. 프로덕션 입력 그대로의 불일치 케이스로 교체 |
| 110 | 사슬취소-O1 | Opus 5 | P2 출고완료 발주주문은 출고장(첫 쓰기, 되돌릴 수 없음)만 지워지고 선택취소에서 막힘 | **채택·수정** | 승인(`cs`)·실행 직전(`ccNextStep` T--) 둘 다에서 유형 셀로 거름. O2 Nit 로 `재고주문` 양성 판정으로 강화 |
| 111 | 사슬취소-O1 | Opus 5 | Nit 중단 시 이미 쓴 건 화면 미갱신 · T-- 바코드 없는 행 승인 통과 · 링크 파싱 실패 사유 오진 | **채택 2·기각 1** | 앞 둘 수정. 파싱 실패 사유는 fail-closed 라 문구만의 문제 — 기록 |
| 112 | 사슬취소-2R | Terra | P2 폴링 마감 직전 시작한 재조회가 12s 뒤 성공으로 오면 그 응답으로 다음 쓰기 | **기각(기록)** | 12s 는 대기 상한이지 응답 유효기한이 아니다. 늦은 응답도 그 시점의 서버 진실(더 새 응답), 다음 쓰기 직전 게이트·[중단] 재확인. 최악은 대기 12s+8s. 4.1.9 같은 루프가 T3 통과 |
| 113 | 사슬취소-O2 | Opus 5 | P2 삭제 POST 타임아웃 배선 3지점이 테스트 밖(변이 3종 생존) | **채택·수정** | 실제 `dcmDelete`·`dcmPostRaw` 추출본으로 abort·fetch init.signal·타이머를 동작 테스트, 변이 3종 KILL |
| 114 | 사슬취소-O2 | Opus 5 | Nit 발주 라벨 집합이 실측 2종뿐(미탐 가능) · cs.balju 라이브 미확인 · 타임아웃 뒤 실제 삭제 시 WAL deleted 누락 | **채택 1·기록 2** | 양성 판정(`재고주문` 요구)으로 전환. 라이브 읽기 재확인(매장 목록 13열·standby 앵커 없음 → 버튼 미주입). WAL 은 before_delete 로 복구 근거 충분 |
| 115 | 사슬취소-X1 | DeepSeek v4-pro | 지적 없음(불변식 9종·상태기계·signal 역호환·테스트 확인) | 채택 — · 기각 — | 교차 1회(사장님 지시로 Fable 없음) |

---

## 회차별 상세

### 주문전표 일괄취소 확장 — 출고완료·입고완료·본사확인 건의 상태 사슬 취소 — SHELL v4.2.7 (2026-09-16)

**대상**: `src/skin.js` §5.11(`ccNextStep`·`ccStepOutcome`·`ccPickDelivIdx`·`ccBuildUnassignUrl`·`ccCellBarcode`·`ccRowIsJaego`·`ccChainLabel`·`ccRequeryReason` 순수부 / `ccDoStandbyOff`·`ccDoUnassign`·`ccFindDelivRow`·`ccDoDelivDelete`·`ccDoStep`·`ccRunCancelBatch` 상태기계·`ccShowApprovalDialog`) · §5.10 `cReadCheckedRows`(cs) · `cBuildStandbyUrl` 상태 인자 · `dcmPostRaw/dcmDelete` 선택 signal · `popup/popup.html`. 테스트 `orderitem-cancel(+25)`·`orderitem-cancel-batch(+22)`·`orderitem-c2b(+1)`. 브랜치 `feat/cancel-chain` → main. 스펙 `2026-09-16-orderitem-cancel-chain-design.md`, 플랜 `…/plans/2026-09-16-orderitem-cancel-chain.md`.

**검수 등급 T3** — 운영 ERP 에 되돌릴 수 없는 쓰기 4종(출고장 삭제·선택취소·본사확인취소·취소)을 사슬로 보낸다. 사장님 지시(Fable 없음·라운드 최소)대로 Terra 반복 + Opus 5 + DeepSeek 교차 1회.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 고위험 | `openai/gpt-5.6-terra` (high) | 2 (P1·P2) | 2 | 0 | $0.1834 |
| O1 | 내부 | Opus 5 xhigh | 1 (P1) + 2 (P2) + Nit 3 | 4 + 2 | 1(기록) | 구독(미분리) |
| 2R | 고위험 재검수 | `openai/gpt-5.6-terra` (high) | 1 (P2) | 0 | 1(기록) | $0.1696 |
| O2 | 내부 재검수 | Opus 5 xhigh | 1 (P2) + Nit 3 | 2 | 2(기록) | 구독(미분리) |
| X1 | 교차 | `deepseek/deepseek-v4-pro` | 지적 없음(불변식 9종·상태기계·signal 역호환·테스트 확인) | — | — | $0.1199 |

**OpenRouter 검수 비용**: Terra $0.3530 + DeepSeek $0.1199. 변이: 루프 6종 + 1R 반영 5종 + O2 배선 3종 = 14종 전부 KILL. 라이브: 읽기 전용으로 실제 `cReadCheckedRows→ccClassifyChecked→ccChainLabel` 를 목록 응답에 돌려 승인창 문구 확인(T--/OS-/O--/TS-/OC- — 매장 계정 세션이라 I-- 는 전부 제외 사유). **쓰기 1건 라이브는 사장님 지정 출고완료 실제 건으로 입회 하에**(미실행).

**교훈**: ① 같은 출처끼리의 대조는 대조가 아니다(Opus O1 P1) — 가드를 만들 때 두 값의 원천이 독립인지 먼저 본다. ② 되돌릴 수 없는 첫 쓰기 뒤에 막히는 경로(막다른 길)는 첫 쓰기 전에 걸러야 한다(Opus O1 P2). ③ 타임아웃 같은 배선은 첫 홉만 테스트하면 뒤 홉 변이가 산다(Opus O2 P2) — 실물 추출본으로 끝까지. ④ 계정 스코프(매장 13열/본사 14열)가 목록 계약을 바꾼다 — 라이브 읽기 확인은 본사 계정 세션에서 한 번 더.


### 주문 가져오기 고객 등록 보강 — 휴대폰 중복 거부 재시도(비고에 번호) · 카페24 정산 차 60%↑ 지인소개 — SHELL v4.2.6 (2026-09-16)

**대상**: `src/orderimport-core.js`(`oiPhoneDupMsg`·`oiReferralRatio`·`oiClientJob`·`oiRunOrder` 고객 단계 재시도) · `src/orderimport-erp.js`(`registerClient` 4번째 인자 `remark`) · `src/orderimport.js`(`toRunOrder`·`custChip`) · 테스트 +10. 브랜치 `feat/orderimport-register-rules` → main. 사장님 규칙 2건(2026-09-16). 스펙 `2026-09-16-orderimport-panel-redesign.md` §5d.

**검수 등급 T2** — 고객 등록 POST 경로(서버 쓰기)와 등록 데이터(마켓 코드)를 바꾸지만 고객 레코드는 되돌리기 쉽고 diff 가 작다. 사장님 지시("검수 너무 많이 하지마")대로 외부 1명(Luna) 1R + Opus 5 1회, 교차 생략. P2-1 수정은 테스트 핀이라 Opus 재검수 없이 변이 확인으로 닫음.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R(무효) | 기본 | `openai/gpt-5.6-luna` (high) | 미수행 — 브리프 생성 스크립트가 `%` 서식 문자('60%')로 죽어 `REVIEW-BRIEF.tmp.md` 가 없었고 검수자가 범위를 못 찾음 | — | — | $0.0627 (낭비) |
| O1 | 내부 | Opus 5 xhigh | 1 (P2) + Nit 3 | 1 | 3(기록) | 구독(미분리) — 브리프 부재를 스크래치패드에서 복원해 검수 완료·Luna 미수행을 프로세스 지적으로 보고 |
| 1R | 기본 | `openai/gpt-5.6-luna` (high) | 0 | — | — | $0.0236 |

**OpenRouter 검수 비용**: $0.0863 (Luna ×2, 그중 $0.0627 은 브리프 부재로 낭비 — 브리프 생성기는 `%` 서식 대신 `replace` 로 고침). 변이 5종(재시도 제거·비고 누락·판정 제거·toRunOrder 원복·재검색 제거) + 어댑터 remark 제거 → 전부 KILL.

**라이브 확인**: 카페24 건(휴대폰 중복 거부 실례)을 다음 파일 실행에서 사장님이 확인(재시도 → 비고에 번호 → 등록 완료). 지인소개 판정은 검토 표 칩으로 실행 전에 보인다.


### 주문 가져오기 패널 리디자인 · 중복 주문장 사전 경고 · 다크모드 제거 — SHELL v4.2.4 (2026-09-16)

**대상**: `src/orderimport.js`(CSS·마크업 전면, 진행 상태 `S.phase/S.progress`, `renderSoft/renderBody/progPatch`, 중복 경고, 사은품 편집) · `src/orderimport-core.js`(`oiOrderSig/oiDupCheck/oiStepLabel/oiEnrichTotal`, 장부 `sig`, 실행 결과 `sig`, `line` log 에 `n`) · `src/skin.js`·`popup/*`(다크모드 삭제만) · 테스트 4파일 + `tests/no-darkmode.test.js` · `manifest.json` 4.2.4. 스펙 `docs/superpowers/specs/2026-09-16-orderimport-panel-redesign.md`, 플랜 `…/plans/2026-09-16-orderimport-panel-redesign.md`. 브랜치 `feat/orderimport-redesign` → main. 사은품 0원(§5c)은 도중에 `fix/gift-zero-price` 로 떼어 4.2.3 으로 먼저 배포하고 브랜치에 병합.

**검수 등급 T2** — 표시가 대부분이지만 §5b 가 실행 집합(기본 체크·전체 체크·확인창)을 바꾼다. 외부 1명(GLM) + Opus 5. **교차는 사장님 지시("검수 너무 많이 하지마", 2026-09-16)로 실행 중 중단·생략.**

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 복잡한 로직 | `z-ai/glm-5.2` | 1 (P3) | 1 | 0 | $0.4532 (10분 초과, 백그라운드) |
| O1 | 내부 | Opus 5 xhigh | 5 (P2) + Nit 4 | 5 + 2 | 2(기록) | 구독(미분리) — ⚠ 검수 도중 내가 사은품 핫픽스로 브랜치를 옮겨 트리가 바뀜(검수자가 `git archive` 로 대상 커밋을 떠서 완료). 내 실수 |
| O2 | 내부 재검수 | Opus 5 xhigh | 2 (P2) + Nit 2 | 4 | 0 | 구독(미분리) — P2-A 는 O1 Nit 수정이 만든 회귀 |
| O3 | 내부 재검수 | Opus 5 xhigh | 0 | — | — | 구독(미분리) |
| X1 | 교차 | `deepseek/deepseek-v4-pro` | 중단(사장님 지시) | — | — | 부분 소모(미분리) |

**OpenRouter 검수 비용**: GLM $0.4532 (+ 중단된 DeepSeek 부분). 렌더 확인은 실제 코드를 가짜 chrome/ERP 위에서 돌리는 하네스(`scratchpad/mock/harness.js`)로 4상태 스크린샷 — 라이브 미접촉. worker 위임 없음.

**채택·수정한 것 (전부 코드로 재현 후)** — 위 #96~#99. 핵심 교훈: ① 큰 목록을 옮길 때 잘림(오늘룩) — 원본을 diff 로 대조할 것 ② 파일 input·검색칸·마켓 픽커처럼 **DOM 에만 사는 값**은 재렌더가 곧 데이터 손실 — 진행 중 갱신은 표만, 값은 상태로 ③ "입력 보호" 같은 UX 배려를 리스너로 구현하면 꼬리(focusout)가 회귀를 만든다 — 단순히 감수하는 쪽이 맞았다.

**라이브 확인**: 4.2.4 배포 뒤 사장님 실제 xls 로 화면 확인 예정(진행 스트립·중복 배너·사은품 0원 완료 대조).


### 사은품 판매가 0 허용 — SHELL v4.2.3 (2026-09-16)

**대상**: `src/orderimport-core.js`(`oiParseRows` gift·`oiLinePayload` 판매가 검사) · `src/orderimport.js`(`toRunOrder` gift·판매가 편집) · 테스트 +2. 브랜치 `fix/gift-zero-price` → main. 사장님 지시("사은품이라 표기된 항목은 판매가 0 이라도 주문이 되도록, 유비샵도 0 주문 가능")를 리디자인 회차에서 떼어 먼저 배포.
**검수 등급 T1** — 소규모, 명시된 업무 규칙, 일반 상품 차단은 그대로. 외부 1명(Luna) 1라운드, 채택 0.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 기본 | `openai/gpt-5.6-luna` (high) | 0 | — | — | $0.0162 |

⚠ 완료 직전 대조(`oiCheckFinal`)의 주문가 0 비교는 서버 행이 '0' 을 돌려준다는 전제(미실측) — 첫 사은품 라이브 등록에서 확인. 다르면 되돌리기(fail-closed).


### 주문 가져오기 '직접 검색' 무반응 버그 — SHELL v4.2.2 (2026-09-16)

**대상**: `src/orderimport.js`(`searchLine`·`onKeydown`·`onChange` q 분기·`lineRow` 검색칸/안내·keydown 배선) · `tests/orderimport-wiring.test.js`(+3건) · `manifest.json` 4.2.2. 브랜치 `fix/orderimport-search` → main.
사장님 제보(2026-09-16): 키워드를 치고 [검색]을 눌러도 반응 없음. 원인: 검색칸 `change` 가 표 전체를 다시 그려 글자가 사라지고 클릭이 옛 버튼에 붙음 + Enter 미처리.

**검수 등급 T1** — 런타임 표시·조회(읽기 GET)만, 쓰기 경로 무변경, 되돌리기 쉬움. 외부 1명(Luna) 최대 2라운드, Opus 5 생략.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 기본 | `openai/gpt-5.6-luna` (high) | 2 (P2·P2) | 1 + 부분 1 | 0 | $0.0119 |
| 2R | 재검수 | `openai/gpt-5.6-luna` (high) | 1 (P2) | 1 | 0 | $0.0301 |

합계 $0.0420. ⚠️ **T1 상한(2라운드)에 닿았고 2R 채택분(#94, 세대 토큰)은 외부 재검수를 받지 않았다** — 파일 선택의 `fileGen` 과 같은 패턴이라 자체 판정으로 닫고 사장님께 보고. 3라운드는 사장님 판단.
⚠️ codex.exe 해시 폴더가 밤사이 바뀌어(`bffc5354…` → `12219cbf…`) 첫 실행이 exit 127 — `~/.codex/config.toml` 의 `CODEX_CLI_PATH` 로 재확인해 재실행(알려진 함정).


### 회전입고 자동화 보강 — 본사반품확인 건너뛰기 · 새 바코드 팝업 강조 — SHELL v4.2.1 (2026-09-15)

**대상**: `src/skin.js` §5.6(`rotStep1Outcome`·`rotNewBarcodeFromCells`·`rotNewBarcodeFromRow`·`rotAfterRowFound/Missing`·`rotServerMsg`·`rotSetResultStatus`, `rotateRun` 3줄, 공용 `ubHighlightPending` 훅 2줄, 회전입고 배선 msg/재적용, `stkRecentAdd/Save` 반환값) · `tests/rotate-flow.test.js`(신규 23건) · `manifest.json` 4.2.1 · `shell-files.json`.
스펙 `docs/superpowers/specs/2026-09-15-rotate-passthrough-newbarcode-design.md`, 플랜 `docs/superpowers/plans/2026-09-15-rotate-passthrough-newbarcode.md`. 브랜치 `feat/rotate-passthrough` → main. 검수 대상은 `git diff f3e8bd3..HEAD -- src/skin.js tests/rotate-flow.test.js manifest.json`.

**검수 등급 T2** — 근거: ①런타임 영향 있음(회전입고 화면 사이드바) ②되돌리기: 회전입고 쓰기(form1.submit)는 기존 경로 그대로이고 사장님 확인대로 서버가 상태를 거부, 새 코드는 표시·보관함용 ③재고 도메인이나 새 쓰기 요청 0 ④다함수 배선·소규모 diff(+~120줄). → 외부 1명(GLM, 최대 3라운드) + Opus 5 + 완료 직전 교차 1회. **Fable 은 사장님 지시(2026-09-15)로 생략.**

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 복잡한 로직 | `z-ai/glm-5.2` | 0 | — | — | $0.4042 |
| O1 | 내부 | Opus 5 xhigh | 1 (P2) + Nit 6 + 관찰 1 | 7 | 0(관찰 1 범위 밖) | 구독(미분리) |
| O2 | 내부 재검수 | Opus 5 xhigh | Nit 3 | 3 | 0 | 구독(미분리) |
| X1 | 교차 | `deepseek/deepseek-v4-pro` | 0 | — | — | $0.0573 (CJK 0) |

**OpenRouter 검수 비용 합계 $0.4615**(`usage_daily` 8.9329 → 9.6320, 사이의 조회 포함). worker 위임 없음 — 메인 세션 직접 구현(오늘 읽기 실측한 DOM 사실에 묶인 소규모 변경). 채택 0 은 1R 에서 바로 나와 외부 라운드는 1회로 닫았고(상한은 할당량이 아니다), O1·O2 반영분은 교차(X1)가 최종 HEAD 에서 봤다.

**채택·수정한 것 (전부 코드로 재현 후)**
- O1 P2: 같은 바코드를 다시 스캔하면 1단계가 "가능한 상태가 아닙니다"로 통과 → form1.submit → 서버 거부 → `?msg=` 재로드인데 열린 회전입고장에 **이전 실행의 A→X 행**이 남아 있어 폴러가 그 행으로 초록 "등록"을 띄웠다 → msg 가 있으면 무시.
- O1 Nit: 재렌더(접기/펼치기·storage 변경)로 결과가 "직전:" 으로 되돌아감 → `rotLastResult` · `<span>2609I8</span><br>F-NF…` 의 textContent 가 붙어 나올 위험 → 자식 노드 공백 join · 변이 3종 생존(초기값·정규식 완화·`r !== tr`) → 테스트 · 낡은 주석 · `stkRecentAdd` 가 조용히 건너뛰어도 "등록" → true/false 반환 · 재고화가 60초 안에 남긴 flag 로 "행 못 찾음" 오경고 → `UB_ROTATE_LAST` 대조.
- O2 Nit: 거부 플래그를 배선에서만 세우면 사이드바가 접힌 채 폴링이 먼저 돌 때 가드가 빠짐 → `rotServerMsg()` 로 URL 직접 읽기 · 주석 노드가 토큰이 됨 → 제외 · 새 실행 시 `rotLastResult` 초기화.

**편향 메모**: GLM(1R)·DeepSeek(X1)이 0 이고 Opus 가 P2 1 + Nit 9 를 냈다 — 셋 다 "코드로 재현" 조건은 지켰고, Opus 지적은 전부 상태줄·표시·테스트 그물 계열(데이터 영향 없음). 외부 자리가 두 회차 연속 0 인 것은 diff 가 작고 서버가 쓰기를 거르는 구조라서로 본다 — 다음 회차에서 외부 0 이 반복되면 선임(GLM 고정)을 의심한다.

**라이브 확인**: 사장님이 실제 회전입고 1건(이미 본사반품확인된 건이면 더 좋음)으로 상태줄 `… → 새바코드 XXXXXX · 본사확인 팝업 강조 등록` 과 팝업 강조를 확인하기로. 결과는 스펙 §3 끝에 1줄 기록 예정.


### 판매처 주문 가져오기(이지어드민 xls → 유비샵 주문장) — SHELL v4.2.0 (2026-09-14 ~ 09-15)

**대상**: `src/orderimport-core.js`(순수 로직 ~830줄) · `src/orderimport-erp.js`(fetch 어댑터) · `src/orderimport.js`(패널 UI) ·
`src/orderimport-xls.js`(SheetJS MAIN 브리지) · `src/background.js`(주입 핸들러) · `manifest.json` 4.2.0 · `src/skin.js`(사이드바 섹션) ·
`popup/*`(스위치·일괄체크·새버전 알림) · `build-shell-index.ps1` · `tests/orderimport-*.test.js` 4파일 + 픽스처.
스펙 `docs/superpowers/specs/2026-09-14-orderimport-design.md`, 플랜 `docs/superpowers/plans/2026-09-14-orderimport.md`.
브랜치 `feat/orderimport` → main. 검수 대상은 `git diff main...HEAD -- <15경로>` 로 좁혔고 `vendor/`·`docs/` 제외.

**검수 등급 T3** — 근거: ①런타임 영향(판매처 계정 주문 화면) ②**되돌리기 어려움**(실제 ERP 에 주문장·고객을 만든다,
되돌리기 경로는 사장님 결정으로 라이브 미실측) ③**위험 도메인 = 주문·데이터 손실·동시성**(로그인 세션당 열린 주문장 1개를
다른 탭/사람과 공유 — 개발 중 실제 사고 1건: 사장님 작업 중인 주문장에 내 줄이 섞여 9호·17호 삭제) ④복잡도 높음(5파일·상태 얽힘).
→ 라운드 무제한(채택 0까지) + Opus 5 + 교차 1회 + Fable 5(되돌리기 어려운 라이브 쓰기 조건).

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 고위험 | `openai/gpt-5.6-terra` (high) | 2 (P1·P1) | 2 | 0 | $0.3970 |
| 2R | 재검수 | `openai/gpt-5.6-terra` | 3 (P1·P1·P2) | 3 | 0 | $0.3497 |
| 3R | 재검수 | `openai/gpt-5.6-terra` | 3 (P1·P1·P2) | 2 + 부분 1 | 부분 1 | $0.5522 |
| 4R | 재검수 | `openai/gpt-5.6-terra` | 5 (P1×3·P2×2) | 5 | 0 | $0.6018 |
| 5R | 재검수 | `openai/gpt-5.6-terra` | 1 (P1) | 1 | 0 | $0.4300 |
| 6R | 재검수 | `openai/gpt-5.6-terra` | 1 (P1) | 1 | 0 | $0.4614 |
| 7R | 재검수 | `openai/gpt-5.6-terra` | 2 (P1·P1) | 2 (1은 배포 단계) | 0 | $0.5159 |
| 8R | 재검수 | `openai/gpt-5.6-terra` | 2 (P1·P2) | 2 | 0 | $0.4862 |
| 9R | 재검수 | `openai/gpt-5.6-terra` | 1 (P1) | 1 | 0 | $0.5169 |
| 10R | 재검수 | `openai/gpt-5.6-terra` | 4 (P1·P2×3) | 3 | **1** | $0.6235 |
| 11R | 재검수 | `openai/gpt-5.6-terra` | 1 (P1) | 1 | 0 | $0.5069 |
| 12R | 재검수 | `openai/gpt-5.6-terra` | 2 (P2·P2) | 2 | 0 | $0.5158 |
| 13R | 재검수 | `openai/gpt-5.6-terra` | 2 (P2·P2) | 2 (+자체 발견 1) | 0 | $0.4439 |
| 14R | 재검수 | `openai/gpt-5.6-terra` | 2 (P1·P2) | 2 | 0 | $0.5570 |
| 15R | 재검수 | `openai/gpt-5.6-terra` | 0 | — | — | $0.4208 |
| O1 | 내부 | Opus 5 xhigh | 5 (P1·P2×4) + Nit | 5 + Nit | 0 | 구독(미분리) |
| O2 | 내부 재검수 | Opus 5 xhigh | 3 (P1·P2·P2) + Nit 2 | 3 + 2 | 0 | 구독(미분리) |
| O3 | 내부 재검수 | Opus 5 xhigh | Nit 2 | 1 | 0(인지 1) | 구독(미분리) |
| X1 | 교차 | `deepseek/deepseek-v4-pro` | **중단**(의견 없이 `Review was interrupted`, exit 0) | — | — | $0.7902(+지연 반영 $0.0284) — 산출물 없음 |
| X1 재시도 | 교차 | `deepseek/deepseek-v4-pro` (탐색 최소화 지시) | 0 (관찰 1) | 0 | 0(문제 없음) | $0.2996 (CJK 오염 0) |
| F1 | 고위험 추가 | Fable 5.1 (`fable-reviewer`) | 5 (P1조건부·P2·P2·Nit·Nit) +Nit 변이 2 | 4 + 2 | **1** | 구독(미분리) — 첫 시도는 세션 한도 429 로 시작 직후 사망(13:10 리셋 후 재시도) |
| F2 | 재검수 | Fable 5.1 | 1 (P2) | 1 | 0 | 구독(미분리) |
| F3 | 재검수 | Fable 5.1 | Nit 1(2항) | 1 | 0 | 구독(미분리) |
| F4 | 재검수 | Fable 5.1 | Nit 1 | 1 | 0 | 구독(미분리) — 반영분(한 줄 `render()`)은 검수자가 지정한 그대로라 추가 라운드 없이 자체 검증(86/86)으로 닫음 |

**OpenRouter 검수 비용**: Terra 15R 합계 **$7.3790**(`usage_daily` 0.1003 → 7.4793) + DeepSeek 교차 **$1.1182**(중단 1회 포함, `usage_daily` 7.6283 → 8.7466) = **$8.4972**.
worker 위임 없음 — 메인 세션 직접 구현(전역 지침 ③: 라이브 실측(Phase 0)·사고 맥락에 강하게 묶인 작업).
⚠️ Terra 15라운드는 이 저장소 최장이다 — 되돌리기 어려운 쓰기 경로라 상한 없이 돌렸고, 1~14R 지적이 전부 유효(기각 1)했다.
채택률이 높았던 이유는 실행기의 실패 분기(응답 유실·세션 경합)가 많아서이고, 매 라운드 "내 수정의 꼬리"(1R→2R→3R 의
completing/lineUnknown, O1→O2 의 sKey 회귀)가 실제로 나왔다 — 재검수 생략 금지 규칙이 또 맞았다.

**채택·수정한 것 (전부 코드로 재현 후)** — 회차 표는 브리프/원장 「누적 판정」 #63~ 참조. 핵심만:
- 실패 분기 fail-closed 체계: `res.completing`(완료 POST 전송 뒤 불명 → 삭제 금지 fatal) · `res.lineUnknown`(줄 POST 불명·귀속 불명 → 삭제 금지 fatal) ·
  되돌리기 직전/줄 사이/완료 직전 plain GET 세션 대조 · 되돌린 뒤 세션 비움 확인 · 완료 직후 전표 목록 사후 검출.
- 판정 강화: 등록 응답 행 스냅샷 전체 일치 · 첫 줄은 tradeJun 빈값 요구 · 20줄 상한 · 정수 금액 · 코드만 다른 유일 새 줄은 내 줄로 되돌림.
- UI/상태: `oiPostRunState`(완료됐을 수 있는 fatal 은 체크 해제+장부 unverified) · 장부 병합 저장 · fileGen · 매핑 메타 승계·학습 · 세션 만료 리다이렉트 검출.
- **O2 P1 은 O1 수정이 만든 회귀**(세션 대조 GET 이 form10 GET 과 완료 POST 사이에 끼어 sKey 규칙 위반) — 스펙 §4 에 "그 GET 과 POST 사이에 다른 GET 을 끼우지 않는다" 를 명문화.

**기각·부분 채택 (근거)**
- 3R Terra "서버측 compare-and-complete/배타 잠금, 불가능하면 자동 완료 금지": 유비샵(Struts 폼 POST)에 원자 연산이 없다. 자동 완료 금지는 사장님 결정(실행 중 조작 금지)으로 기각. 사후 검출 + plain GET 대조로 대응.
- 10R Terra "고객 등록 응답 유실 → fatal": 같은 고객이 같은 배치에 다시 나오면 이름 검색이 등록분을 재사용 — 중복 고객 없음. 고객 등록은 주문 쓰기가 아니다.
- O3 Opus "키 발급 GET 에서 tradeJun 명시를 빼면 대조 GET 이 불필요(창 0)": 전제 2개(세션 반영 GET 의 form1/form10 렌더)가 **미실측** — 다음 라이브 읽기 때 재서 결정. 지금 구조는 스펙 §4 와 일치.

**라이브 실측(Phase 0·실제 실행)**: 2026-09-14 실제 주문 4건(1건은 동시성 사고로 중단) · 2026-09-15 당일 주문 10주문장·11줄 전부 이지어드민과 일치(사장님 확인).
되돌리기(`orderItemDelete`)만 미실측 — 사장님이 실제 건에서 직접 확인하며 진행하기로 결정.

**편향 메모**: Terra 단독 15라운드(고위험 자리 고정)라 회차 안 비교 대상이 없다. 교차 DeepSeek 과 Fable 이 Terra·Opus 가 못 본 것을 냈는지가 이 회차의 편향 지표 — **Terra·Opus 18라운드가 닫은 뒤 Fable 이 P1 조건부 1건(되돌리기 초과 삭제 미검출) + P2 2건(실행 중 패널 조작·진행 중 enrich)을 단독 적발했다** — 셋 다 '스텁 계약이 틀렸을 때'와 'UI 가 같은 세션에 요청을 끼우는' 각도로, Terra(실행기 분기)·Opus(테스트 고정·sKey 규칙)와 겹치지 않았다. DeepSeek 은 이번엔 값을 못 했다(중단 1회 + 0건). 편향 신호: 없음 — 자리마다 다른 것을 냈다.


### 기초상품관리 자동 포커스 — app-files v2.9.10 (2026-09-14)

**대상**: `src/content.js`(+38줄, masterItemList.do 에서 로드 후 `searchWord2` 포커스) · `app-files.json`(v2.9.10 해시). 사장님 지시.
main 직접 커밋 `9b139d5` → `4e24e72` → `e6f2cfe`. 검수 대상 `review --commit`/`--base HEAD~2`(diff 만).

**검수 등급 T1** — 근거: ①런타임 영향 있음(매장 상시 화면) ②되돌리기 쉬움(push 로 즉시 원복, 서버 쓰기 없음)
③위험 도메인 아님(포커스뿐) ④단일 파일 38줄. → 외부 1명(Luna) 최대 2라운드, Opus 5 생략, 교차 없음.
원장 「누적 판정」은 붙이지 않았다 — `review --commit` 은 프롬프트를 못 싣고, 기존 판정(바코드 필터·취소)과 무관한 diff 라 재발 여지가 없다.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 기본 | `openai/gpt-5.6-luna` (high) | 1 (P1) | 1 | 0 | $0.0174 |
| 2R | 재검수 | `openai/gpt-5.6-luna` (high) | 2 (P2·P2) | 1 | 1 | $0.0236 |

⚠️ **T1 상한(2라운드)에 닿았다.** 2R 채택분(#62)의 수정 `e6f2cfe` 는 외부 재검수를 받지 않았다 — 가드 한 줄을
강화한 것(`isControl`)이라 자체 판정으로 닫고 사장님께 보고한다. 3라운드를 돌릴지는 사장님 판단.

**편향 메모**: Luna 3건 중 2건 채택. 1R P1 은 "코드" 가 아니라 **배포 배관** 지적이었고(원장 #39 와 같은 계열),
이번엔 검수 순서(인덱스 갱신 전 검수)를 내가 잘못 잡아 나온 것이다 — 다음부턴 `build-app-index` 후에 검수를 건다.


### 주문전표 일괄취소 [일괄취소] 버튼 — SHELL v4.1.9 (2026-09-11)

**대상**: `src/skin.js`(§5.11 신규 ~330줄 + `fetchOrderRow` sKey 필드 + 배선 2줄) · `popup/popup.html`(스위치 라벨) ·
`manifest.json` 4.1.9 · `tests/orderitem-cancel*.test.js`(신규 3파일). 스펙 `docs/superpowers/specs/2026-09-11-orderitem-bulk-cancel-design.md`.
브랜치 `feat/orderitem-bulk-cancel` → main. 검수 대상은 `git diff main...HEAD -- <5파일>` 로 좁혔다.

**검수 등급 T3** — 근거: ①런타임 영향 있음(매장 상시 화면의 툴바 버튼) ②**되돌리기 어려움**(ERP 원문 "취소 된 주문서는
복구되지 않습니다" — 취소 GET 은 서버 쓰기) ③**위험 도메인 = 주문·데이터 손실** ④복잡도 중간(단일 파일, 작업C 배관 재사용).
→ ②③ 때문에 T3. 라운드 무제한(채택 0까지) + Opus 5 + 교차 1회 + Fable 5(되돌리기 어려운 라이브 쓰기 조건).

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 고위험 | `openai/gpt-5.6-terra` (high) | 1 (P1) | 1 | 0 | $0.1623 |
| 2R | 재검수 | `openai/gpt-5.6-terra` | 2 (P1·P2) | 2 | 0 | $0.2907 |
| 3R | 재검수 | `openai/gpt-5.6-terra` | 2 (P1·P1) | 1 | **1** | $0.3345 |
| 4R | 재검수 | `openai/gpt-5.6-terra` | 1 (P1) | 1 | 0 | $0.1428 |
| 5R | 재검수 | `openai/gpt-5.6-terra` | 0 | — | — | $0.2411 |
| O1 | 내부 | Opus 5 xhigh | 4 (P2) | 4 | 0 | 구독(미분리) |
| O2 | 내부 재검수 | Opus 5 xhigh | 1 (P2) | 1 | 0 | 구독(미분리) |
| X1 | 교차 | `deepseek/deepseek-v4-pro` | 0 | — | — | $0.8024 (1,132초 — 느림, CJK 오염 0) |
| F1 | 고위험 추가 | Fable 5.1 (`fable-reviewer`) | 4 (P1·P2×2·Nit) +P2 실측요구 1 | 5 | 0 | 구독(미분리) |
| F2 | 재검수 | Fable 5.1 | 1 (Nit) | 0 | 0(인지) | 구독(미분리) |

**OpenRouter 검수 비용 합계 $1.9738** (`usage_daily` 0.4163 → 3.0873, 사이의 가격 조회 포함).
worker 위임 없음 — 메인 세션 직접 구현(전역 지침 ③: 작업C 코드를 이미 전부 읽어 메인 컨텍스트 의존도가 높은 작업).

**채택·수정한 것 (전부 코드로 재현 후)**
- 1R Terra: 배치 진행 중 툴바 재클릭이 진행 창을 지워 [중단] 을 잃음 → 진입점 busy 가드 + `dataset.ubRunning` 보존.
- 2R Terra: 승인창 [취소 진행] 이 `isTrusted` 미검사(페이지 스크립트 `.click()` 으로 쓰기 시작 가능) → 가드. / 건별 fresh sKey 불변식 미고정 → 테스트.
- 3R Terra: sKey 대기(최대 8초) 중 상태 변경 → dispatch 직전 재검증(뒤에 Opus 반영으로 "상태+키 한 응답" 구조로 대체).
- 4R Terra: 대기 중 게이트 OFF/[중단] 이어도 GET → dispatch 직전 재확인.
- O1 Opus: 중단 시 요약 '처리 완료' 오표시 / busy 세움 미고정 / **sKey 별도 GET 과 중간 렌더 상호작용 가설** → `fetchOrderRow` 가
  응답의 sKey 를 함께 반환해 상태·키를 한 응답에서(네이티브와 같은 계약, 라이브 읽기 실측) / 닫기 라벨.
- O2 Opus: `fetchOrderRow` sKey 배관 미고정(변이 2종 생존) → 실제 함수를 가짜 fetch/DOMParser 로 돌리는 테스트 3건.
- F1 Fable: **취소 `seq` 와 `del()` 인자 동일성 미실측 + 같은 응답의 [취소] 링크를 안 봄(P1)** → 라이브 읽기 실측(332/332, 프로덕션 경로 `tr.outerHTML` 로 327/327) + `ccRowCancelSeq` 링크 인자 EXACT 가드 / 무필터 재조회의 OC- 반환 실측 / 툴바 주입·배선·배경 클릭 테스트 / 타이머 Nit.

**기각(근거)**
- 3R Terra "document-local `cBatchBusy` 라 교차 탭 중복 실행 → SW 전역 잠금": 재현하려면 같은 PC 두 탭에서 사람이 같은 행을
  각각 승인해야 하고(승인창은 신뢰된 클릭 필수), 그래도 각 탭이 쓰기 직전 재조회로 O-- 를 확인해 두 번째 GET 은 이미 OC- 인
  주문에 간다(오취소·데이터 손실 아님). 네이티브 UI 도 같은 노출. SW 전역 잠금은 잠금 누수·TTL 등 새 위험이 더 크고,
  검수 통과·운영 중인 작업C 도 같은 설계.

🔴 **이 회차의 교훈** — 라운드마다 나온 P1 이 전부 **"내 앞 라운드 수정의 꼬리"** 였다(재진입 가드 → 진행 버튼 신뢰 → 대기 창 →
대기 중 게이트 → 집계). 되돌릴 수 없는 쓰기 자동화는 **"대기(await) 뒤에는 무엇이든 다시 확인한다"** 를 처음부터 규칙으로
박았어야 했다. 최종 구조는 "상태·키를 한 응답에서 받고, 그 뒤 await 없이 게이트·중단만 보고 GET" 이라 창이 네트워크 왕복 하나다.

**최종**: `node --check` OK · `node --test tests/*.test.js` **240 pass** · 변이 검증 누적 20종 KILL · manifest/shell-files 4.1.9 정합 ·
`loader-integrity` Task1~4 OK. 라이브 쓰기 1건 검증은 push 후 사장님 지정 건으로(스펙 §6).

### 매입처 목록 실시간 검색 + 표 가독성 (2026-09-04)

**대상**: `src/skin.js`(+485) · `tests/factory-live-search.test.js`(신규) — 매입처관리·매입처검색 2화면에
커서 자동포커스 + 엔터 없는 실시간 검색 + 표 가독성 디자인.

**검수 등급 T2** — 근거: ①런타임 영향 있음(매장 상시 사용 화면) ②되돌리기 쉬움(revert+재배포,
라이브 마이그레이션 아님) ③위험 도메인 아님(인증·결제·PII·데이터 손실 없음, POST 는 목록 조회 전용)
④복잡도 중간(단일 파일이나 DOM 교체·비동기 프리페치·고정헤더 생명주기가 얽힘).
→ ③이 낮아 T3 아님, ④ 때문에 T1 로 내리지 않음.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 넓게 | `gpt-5.6-luna` | 2 | 2 | 0 | $0.1001 |
| 1R | 내부 | Opus 5 xhigh | 6 | 5 | 1 | 구독(미분리) |
| 2R | 재검수 | `gpt-5.6-luna` | 1 | 1 | 0 | $0.1606 |
| 2R | 내부 | Opus 5 xhigh | 6 | 6 | 0 | 구독(미분리) |
| 3R | 교차 | `deepseek/deepseek-v4-pro` | 1 (P0 주장) | 0 | **1** | $0.1592 |
| 3R | 내부 | Opus 5 xhigh | 6 | 4 | 2 | 구독(미분리) |

**OpenRouter 검수 비용 합계 $0.4199** (worker 비용 별도: qwen 1차 실패 $0.3960, GLM 3회 미분리).

**worker 라우팅 — 승격 있었음**: 1차 `qwen/qwen3-coder-next` **실패·중단**(21분) → `z-ai/glm-5.2` 3회.
qwen 실패 사유: 5,928줄 파일 직접 편집을 포기하고 패치 스크립트를 만들다 **정규식 역슬래시가 두 겹**이 됐고
(`/\\s+/` → 공백 제거 사망), 금지한 `AUTO_FOCUS_PAGES` 기존 3줄을 갈아엎었으며, 테스트 파일이 0바이트,
저장소 루트에 임시파일 7개. **라우팅 표의 "명세 고정된 긴 코드"=GLM 행이 맞았다**(Advisor 오배정).

🔴 **자동 검사가 전부 통과한 상태에서 디자인 결함 4건이 남아 있었다** — 헤더 흰배경으로 **글자 소실**,
가로 패딩이 **매입처명 열을 193px→88px 로 반토막**, Pretendard 미적용, `position:sticky` 가
**쿼크모드라 원리적으로 불가**(통제 실험: div=OK / td=FAIL). **스크린샷 없이 완료했으면 그대로 나갔다.**

🔴 **변이 검증에서 두 번 뚫렸다** — ①`flOnInput` 본문 전체를 훑어 디바운스 게이트가 진입점 게이트 검사를
대신 만족 ②`runWiringChecks` 가 실제 소스에 한 번도 적용되지 않음. 최종 **변이 19종 전부 KILLED**.
하네스는 **변이가 실제로 적용됐는지 문자수 변화로 먼저 확인**한다(패턴 불일치를 '생존'으로 오독한 전례).

**최종**: `node --check` OK · factory-live-search 48 pass · 저장소 26개 테스트 파일 전부 exit 0 ·
manifest/shell-files 4.1.8 정합 · `loader-integrity` Task1~4 OK.

| 라운드 | 자리 | 모델 | 지적 | 채택 | 기각 | 비용 |
|---|---|---|---|---|---|---|
| 1R | 내부 | Opus 5 xhigh | 4 | 3 (+2 보류→구현) | 0 | 구독(미분리) |
| 1R | 넓게 | `gpt-5.6-luna` | 2 | 2 | 0 | $0.0404 |
| 1R | 깊게 | `kimi-k2.7-code` | 0 | — | — | $0.1773 |
| 1R | 재검수 | `glm-5.2` | 0 | — | — | $0.3840 |
| 1R | 교차 | `deepseek-v4-pro` | 0 | — | — | $0.4659 (+타임아웃 폐기 $0.89) |
| 2R | 넓게 | `gpt-5.6-luna` | 3 | 2 | 1 | $0.0720 |
| 2R | 재검수 | `glm-5.2` | 0 | — | — | $0.0902 |
| 3R | 내부 | Opus 5 xhigh | 7 | 7 | 0 | 구독(미분리) |
| 4R | 넓게 | `gpt-5.6-luna` | 4 | 2 | 2 | $0.0554 |
| 4R | 재검수 | `glm-5.2` | 0 | — | — | $0.1352 |
| 4R | 교차 | `deepseek-v4-pro` | **폐기** | — | — | $0.2703 |
| 5R | 내부 | Opus 5 xhigh | 7 | 7 | 0 | 구독(미분리) |

**누적 OpenRouter 비용 ≈ $2.58** (`usage_daily` 6.38 → 9.47, 검수 외 조회 포함).

🔴 **4R DeepSeek 런은 전역 규칙대로 폐기했다** — 출력에서 CJK 표의문자 134건이 검출됐다.
진단상 **모델 오염이 아니라 셸 에코의 한글이 CP949 로 깨진 mojibake** 다(오염 줄 36개 중 33개가
replacement 문자를 동반, 16개가 소스 코드 에코 줄). 그래도 fail-closed 규칙대로 판정에 쓰지 않았다.
⚠️ 이 경로로 DeepSeek 을 다시 쓸 거면 **codex 출력 인코딩을 UTF-8 로 고정**하는 것부터 확인해야 한다.

### 편향 관찰 (모델별 성적)

- **가장 무거운 건은 전부 내부 Opus 5 가 잡았다** — 진단 신호 소멸(#3), 라벨 폴백의 데이터 파괴(#13),
  사이드바 경고가 사실상 안 뜸(#22), 테스트가 기능 무력화를 놓침(#23~26).
  외부 패널은 그중 어느 것도 못 냈다. **내부 검수 자리를 빼면 안 된다.**
- **Luna 는 세 라운드에서 9건을 내 6건이 채택됐다.** 최저가 자리가 값을 한다.
- **GLM 은 세 라운드 연속 0건**($0.61 소진). 이 저장소에서는 값을 못 하고 있다 —
  다음 회차에도 0 이면 자리를 빼는 것을 검토할 것.
- **K2.7·DeepSeek 도 이 저장소에서는 단독 적발 0.**
- 자체 변이 하네스가 낸 것(#12)도 있다. **변이 검증은 모델 검수와 겹치지 않는 값을 낸다.**
